<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalExam"
        tools:targetApi="31">
        <activity
            android:name=".mainPage.Search"
            android:exported="false" />
        <activity
            android:name=".job.JobDetail"
            android:exported="false" />
        <activity
            android:name=".appliedJob.AppliedJob"
            android:exported="false" />
        <activity
            android:name=".mainPage.MainPage"
            android:exported="true" >

                        <intent-filter>
                            <action android:name="android.intent.action.MAIN" />

                            <category android:name="android.intent.category.LAUNCHER" />
                        </intent-filter>
        </activity>
        <activity
            android:name=".signin_signup.SignIn"
            android:exported="false">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>
        <activity
            android:name=".signin_signup.SignUp"
            android:exported="false">
        </activity>
    </application>

</manifest>